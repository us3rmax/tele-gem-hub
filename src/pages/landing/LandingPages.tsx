import LandingPage from "./LandingPage";
import { LANDING_PAGES } from "./landingPagesData";

// /telegram-putaria
export function TelegramPutariaPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/telegram-putaria")!;
  return <LandingPage {...data} />;
}

// /grupos-putaria-telegram
export function GruposPutariaTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/grupos-putaria-telegram")!;
  return <LandingPage {...data} />;
}

// /telegram-porno
export function TelegramPornoPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/telegram-porno")!;
  return <LandingPage {...data} />;
}

// /telegram-xxx
export function TelegramXxxPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/telegram-xxx")!;
  return <LandingPage {...data} />;
}

// /grupos-telegram-18
export function GruposTelegram18Page() {
  const data = LANDING_PAGES.find((p) => p.rota === "/grupos-telegram-18")!;
  return <LandingPage {...data} />;
}

// /novinhas-telegram
export function NovinhasTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/novinhas-telegram")!;
  return <LandingPage {...data} />;
}

// /vazados-telegram
export function VazadosTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/vazados-telegram")!;
  return <LandingPage {...data} />;
}

// /onlyfans-telegram
export function OnlyfansTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/onlyfans-telegram")!;
  return <LandingPage {...data} />;
}

// /canal-de-putaria
export function CanalDePutariaPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/canal-de-putaria")!;
  return <LandingPage {...data} />;
}

// /telegram-proibido
export function TelegramProibidoPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/telegram-proibido")!;
  return <LandingPage {...data} />;
}

// /telegram-vazados
export function TelegramVazadosPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/telegram-vazados")!;
  return <LandingPage {...data} />;
}

// /putaria-brasileira
export function PutariaBrasileiraPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/putaria-brasileira")!;
  return <LandingPage {...data} />;
}

// /chat-sexo-telegram
export function ChatSexoTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/chat-sexo-telegram")!;
  return <LandingPage {...data} />;
}

// /xvideos-putaria
export function XvideosPutariaPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/xvideos-putaria")!;
  return <LandingPage {...data} />;
}

// /video-sexo-telegram
export function VideoSexoTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/video-sexo-telegram")!;
  return <LandingPage {...data} />;
}

// /mulheres-nuas-telegram
export function MulheresNuasTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/mulheres-nuas-telegram")!;
  return <LandingPage {...data} />;
}

// /videos-eroticos-telegram
export function VideosEroticosTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/videos-eroticos-telegram")!;
  return <LandingPage {...data} />;
}

// /links-telegram
export function LinksTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/links-telegram")!;
  return <LandingPage {...data} />;
}

// /xvideos-porno-telegram
export function XvideosPornoTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/xvideos-porno-telegram")!;
  return <LandingPage {...data} />;
}

// /video-porno-telegram
export function VideoPornoTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/video-porno-telegram")!;
  return <LandingPage {...data} />;
}

// /porno-gratis-telegram
export function PornoGratisTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/porno-gratis-telegram")!;
  return <LandingPage {...data} />;
}

// /putaria-brasileira-telegram
export function PutariaBrasileiraTelegramPage() {
  const data = LANDING_PAGES.find((p) => p.rota === "/putaria-brasileira-telegram")!;
  return <LandingPage {...data} />;
}
